import styles from './StartPage.module.scss';

function StartPage() {
    return (
        <div className={styles.StartPage}>
            <h1>Start Page</h1>
        </div>
    );
}

export default StartPage;
